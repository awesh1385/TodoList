<!-- EJS means Embbeded JS Templete -->
<!--  this are scriptlet tags where we can write js <% %>  
    <% if(listTitle==="Sunday" || listTitle==="Saturday" ){%>
        <h1 style="color: crimson;">It's a <%= listTitle %>!</h1>
        <% }else{ %>
            <h1 style="color: blue;">It's a <%= listTitle %>!</h1>
            <% }%> -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List </title>
    <link rel="stylesheet" href="css/style.css">
</head>

<body>
    <div class="box" id="heading">
        <h1>
            <%=listTitle%>
        </h1>
    </div>

    <div class="box">

        <%newListItems.forEach(function(Item){%>
            <form action="/delete" method="post">
            <div class="item">
                <input type="checkbox" name="checkbox" value="<%=Item._id %>" onchange="this.form.submit()">
                <p>
                    <%= Item.name %>
                </p>
            </div>
            <input type="hidden" name="listName" value="<%= listTitle %>"></input>
        </form>
            <%})%>
                <form method="post" action="/" class="item">
                    <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
                    <button type="submit" name="list" value="<%=listTitle%>">
                        +
                    </button>
                </form>
    </div>


</body>

</html>

